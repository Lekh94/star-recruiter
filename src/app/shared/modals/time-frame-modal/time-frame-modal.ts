import { Component, OnInit, Input } from "@angular/core";

@Component({
    selector: 'time-frame-modal',
    templateUrl: 'time-frame-modal.html',
})

export class TimeFrameModalsComponent implements OnInit {

    ngOnInit() { }
}